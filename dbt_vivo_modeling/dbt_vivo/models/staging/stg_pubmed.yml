version: 2

models:
  - name: stg_pubmed
    description: "Staging table for stg_pubmed table."
    columns:
      - name: id
        description: "Unique identifier for the stg_pubmed."
        tests:
          - not_null
          - unique

     